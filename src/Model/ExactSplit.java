package Model;

import java.util.List;

public class ExactSplit extends Split {

    public ExactSplit(List<Double> amt, List<User> user, Double totalAmount){
        super(amt,user,totalAmount);
    }
    
}
